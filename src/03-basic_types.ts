// Type system
let count: number = 12;
let str: string = 'abc';
let isVerified: boolean = true;

enum CustomerState { CREATED = 'Created', VERIFIED = 'Verified', REJECTED = 'Rejected' };

let chaos: any = 12;
chaos = 'abc' + 100 + true + [];