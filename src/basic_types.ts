let count: number = 12;
let customerName: string = 'DOTP';
let isVerified: boolean = false;

let implicitNumber = 12;

// Enums
enum CustomerState {
  CREATED = 'Created',
  VERIFIED = 'Verified',
  REJECTED = 'Rejected'
}

// Mayhem and chaos
let disasterCount: any = 123;
disasterCount = 'ooooh nooo'